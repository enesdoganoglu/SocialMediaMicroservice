server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/enesdoganoglu/java7ConfigServerGit
          default-label: main
          skip-ssl-validation: true
          username: enesdoganoglu
          password: ${java7configpassword}